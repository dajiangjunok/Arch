import { ReferencePage, referenceMetadata } from "@/app/_reference/reference-page";

export const metadata = referenceMetadata.partners;

export default function PartnersRoute() {
  return <ReferencePage page="partners" />;
}
